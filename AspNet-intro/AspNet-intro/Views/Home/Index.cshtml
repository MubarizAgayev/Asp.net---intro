@model int[];
@{
    int count = 100;
    List<string> names = new List<string>() { "Shaiq", "Konul", "roya" };
}
<h1>Home page</h1>

<section id="home">
    <div>
        <h3>Started backend</h3>
        <span></span>
        <ul>
            @for(int i =0; i < 15; i++)
            {
                <li>@count</li>
                count++;
            }
        </ul>
        @foreach(var item in names)
        {
            <h1>@item</h1>
        }

        @foreach (var item in Model)
        {
            <h1>@item</h1>
        }
    </div>
</section>

<script>
    console.log("fdfd");
</script>
